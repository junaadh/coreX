struct Person {
  name: string,
  age: i32
}

/// Token Kind ts
enum TokenKind {
  Slash,
  Star,
}

fn main() {
  var count = 0;

  if count == 0 {
    count = 1;
  } else {
    count = 2;
  }

  while count < 3 {
    count += 1;
  }

  let maybe_name = "corex";
  let name = maybe_name ?? "guest";
  let label = count > 1 ? "hot" : "cold";

  let user = Person { name: "User 1", age: 30 };
  let age = user.age;

  let token = .Slash;
  let token2 = TokenKind.Star;
}
